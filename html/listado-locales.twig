{% extends "master.twig" %}

{% block head %}
    {{ parent() }}

    {% include 'script-mapa.twig' %}
{% endblock %}

{% block contenido %}
    <section class="container">
        <div class="row titulo">
            <div class="col-md-4">
                <div class="tit2">DONDE COMPRAR - Mapa</div>
            </div>
            <div class="col-md-8">
                {% if(logueado) %}
                    {% include 'desplegar-menu.twig' %}
                {% endif %}
            </div>
        </div>
        <div class="row">
            <input id="pac-input" class="controls" type="text" placeholder="Buscar">
        </div>
        <div class="contenido-mapa row"> 
            <div class="col-md-4">
                <div class="listado">
                    <h4>Listado de locales</h4>
                    <ul class="listado-locales">
                        <!--
                        <li>
                            <p>Nombre del local <br> Calle 48 Nro 650, La Plata</p>
                            <div class="botones">
                                <form action='{{paths.PATH_CONTROLLER}}controladorAdmin.php?seccion=editar-local' method='post'>
                                    <input type="submit"  value ="Editar" class="btn btn-primary"></input>
                                </form>
                                <form action='{{paths.PATH_CONTROLLER}}controladorAdmin.php?seccion=editar-local' method='post'>
                                    <input type="button"  value ="Cancelar" class="btn btn-default"></input>
                                </form>
                            </div>
                        </li>
                    -->
                    </ul>
                </div>
            </div>
            <div class="col-md-8">
                <div class="mapa-locales" id="map_canvas" ></div>
            </div>
        </div>
        

    </section>
{% endblock %}