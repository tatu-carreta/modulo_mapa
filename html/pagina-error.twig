{% extends "master.twig" %}

{% block contenido %}
    <section class="container">
        <h1>Página de Error</h1>
        <p>{% if(texto is defined) %}{{ texto }}{% else %}Error.{% endif %}</p>
    </section>
{% endblock %}