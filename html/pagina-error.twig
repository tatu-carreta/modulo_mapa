{% extends "master.twig" %}

{% block contenido %}
    <section class="container">
        <div class="tit2">Página de error</div>
        <p>{% if(texto is defined) %}{{ texto }}{% else %}{% endif %}</p>
    </section>
{% endblock %}