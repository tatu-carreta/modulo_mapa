{% extends "master.twig" %}

{% block contenido %}
    <section class="container">
        <h1>Login</h1>
        <p>{% if(error is defined) %}{{ error }}{% endif %}</p>

        <form action="{{ paths.PATH_CONTROLLER }}controladorAdminModel.php" method="post">
            <input type="text" name="usuario" required="true" placeholder="Nombre de Usuario">
            <input type="password" name="clave" required="true" placeholder="Contraseña">
            <input type="hidden" name="section" value="login">
            <input type="submit" value="Ingresar">
        </form>
    </section>
{% endblock %}