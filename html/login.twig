{% extends "master.twig" %}

{% block contenido %}
    <section class="container">
        <div class="tit2">LOGIN</div>
        <div class="error">
            <p>{% if(error is defined) %}{{ error }}{% endif %}</p>
        </div>
        <div class="row">
            <div class="col-md-4">
                <form action="{{ paths.PATH_CONTROLLER }}controladorAdminModel.php" method="post">
                    <div class="form-group">
                        <input class="form-control" type="text" name="usuario" placeholder="Nombre de usuario" required="true">
                    </div>
                    <div class="form-group">
                        <input class="form-control" type="password" name="clave" placeholder="Contraseña" required="true">
                    </div>
                    <input type="hidden" name="section" value="login">
                    <input type="submit" value="Ingresar"  class="btn btn-primary">
                </form>
            </div>
        </div>
    </section>
{% endblock %}